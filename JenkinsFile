pipeline {
    agent {
        label 'master'
    }
    stages {
        stage('Deploy') {
            steps {
                sh "sudo npm install -g serve"
                sh "sudo serve -s build"
            }
        }
    }
}